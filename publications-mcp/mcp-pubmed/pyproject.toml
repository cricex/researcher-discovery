[project]
name = "mcp-pubmed"
version = "0.1.0"
description = "MCP server for the PubMed E-utilities API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "fastmcp>=2.0.0",
    "python-dotenv>=1.0.1",
]
