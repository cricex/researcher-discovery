# Project Context

- **Owner:** msftsean
- **Project:** orchestration-agent — An orchestration agent that routes user intents to specialized agents, manages an agent registry, and aggregates responses
- **Stack:** TypeScript, Node.js
- **Work Areas:** Orchestrator Core, Intent Classifier, Agent Registry, Response Aggregator, UI, Integration Testing
- **Created:** 2026-03-05

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- **Sectioned aggregation (T021+T022):** `DefaultAggregator` now returns an object satisfying both `AggregatedResponse` and `OrchestrationResult`. Sections are a `Map<string, string>` keyed by `agentId`; empty-content and error responses are excluded from sections but error agents remain in `metadata.agentsInvoked`. Citations are extracted from `<cite>tag</cite>` patterns in content and from `metadata.citations` arrays, deduplicated by exact string match, and parsed into `{ raw, sourceType, sourceId }` by splitting on the first underscore. Confidence is a simple average of successful-agent confidences only. All 12 T018 aggregator tests pass green.

- **Agent HTTP contract types** live in `src/specs/agent-contract.ts`. These define the wire-format shapes (AgentRequest, AgentContractResponse, AgentErrorResponse, AgentHealthResponse) and AgentErrorCode enum. Property names are camelCase; the HTTP client layer is responsible for snake_case mapping. Spec source: `specs/001-orchestration-agent/contracts/agent-contract-spec.md`.
- The existing specs style uses JSDoc on every exported type/interface, module-level doc comments, and `.js` extensions in barrel exports. Follow this pattern for any new spec files.
- **Team Updates (Phase 1 Setup):** River pivoted IntentCategory to research domain (EXPERTISE_DISCOVERY, RESEARCH_OUTPUT, COLLABORATION_INSIGHT, POLICY_COMPLIANCE, GENERAL) with new `context` field on ClassifiedIntent. Wash extended response.ts with Citation, OrchestrationResult, AgentErrorEntry — all 15 tests passing. New shared contract contracts are aligned for orchestrator core implementation.
- **Agent endpoint config** lives in `src/agents/endpoints.ts`. `AgentEndpointConfig` interface defines connection details (id, name, url, healthUrl, timeoutMs, enabled). `DEFAULT_AGENT_ENDPOINTS` is a `Map<string, AgentEndpointConfig>` with the three known agents (expertise_discovery :5001, research_output :5002, policy_compliance :5003).
- **HTTP agent client** lives in `src/agents/client.ts`. `HttpAgentClient` class uses native `fetch` with `AbortController` timeouts. It maps camelCase TypeScript types ↔ snake_case wire format via internal `toWireRequest`/`fromWireResponse`/`fromWireError` functions. `checkHealth` never throws — returns `status: 'unhealthy'` on failure. `callAgent` throws typed `AgentErrorResponse` on timeout, HTTP errors, and network failures.
- **Team Updates (Phase 2 Foundation):** River created `src/orchestrator/classifier/routing-rules.ts` with Map<IntentCategory, string[]> for four domain categories (EXPERTISE_DISCOVERY, RESEARCH_OUTPUT, COLLABORATION_INSIGHT, POLICY_COMPLIANCE). Zoe expanded test helpers with createMockAgentResponse, createMockAgentRequest, createStubHttpAgent factories and updated registry.test.ts for new enum values — all 15 tests passing. Phase 2 foundation infrastructure complete.
- **FastAPI backend** created at `src/api/main.py`. Wraps the Python `Orchestrator` class from `src/orchestrator/orchestrator.py`. Uses `sys.path` manipulation to resolve the import from `src/api/` → `src/orchestrator/`. Constructor: `Orchestrator(use_mock_agents=True)`. Key method: `route_query(query)` returns `OrchestrationResult` dataclass with `.response`, `.agents_invoked`, `.citations`, `.confidence`, `.reasoning`, `.metadata`. Cleanup via `close()`. Modernized for Pydantic v2 (`model_dump()`) and FastAPI lifespan context manager. Endpoints: `/`, `/health`, `POST /api/v1/query`, `GET /api/v1/session/{id}`, `DELETE /api/v1/session/{id}`, `GET /api/v1/agents`, `WS /ws/orchestration`. CORS allows ports 3000 and 5173.
- **Wave 1 Cross-Team (2026-03-05T20:30):** FastAPI backend integration with Wash's React frontend (ports 3000 Vite dev, 5173 CORS). Kaylee's B1 is foundational for Wash's B2/B3 and Mal's B6/B7 integration docs. River's T010 tests use routing-rules from T006 (Kaylee baseline from Phase 2). All services orchestrated in parallel via dual-track Wave 1 delivery.
- **HttpAgent adapter** (T014) lives in `src/agents/http-agent.ts`. Implements `Agent` by wrapping `AgentEndpointConfig` + `HttpAgentClient`. Uses a `CATEGORY_MAP` to derive `IntentCategory` from config id. `canHandle` matches intent category; `execute` builds an `AgentRequest`, calls `callAgent()`, and maps `AgentContractResponse` → `AgentResponse` with citations in metadata. Error path returns `status: 'error'` with the message extracted from the thrown `AgentErrorResponse`.
- **AgentRegistry convenience methods** (T015): `registerEndpoint(config, client?)` creates an `HttpAgent` and registers it in one call. `registerDefaults()` iterates `DEFAULT_AGENT_ENDPOINTS` and registers all agents using a shared `HttpAgentClient`, skipping any already-registered ids. Both added to `src/agents/registry.ts`.
- **ResponseAggregator extracted (B5):** `src/orchestrator/aggregator.py` is the dedicated aggregation module. `ResponseAggregator` class owns `aggregate_responses()` → `AggregatedResult` dataclass (sections, citations, confidence, metadata, agents_used). Domain sectioning via `_section_by_domain()` groups output into Expertise/Research/Collaboration/Policy. Citation dedup is order-preserving exact-match. Confidence is weighted average of per-agent confidence × success ratio. `render_markdown()` static method converts `AggregatedResult` → flat markdown string. `orchestrator.py` now imports and delegates to this class instead of inline `_aggregate_responses`. The FastAPI layer (`src/api/main.py`) needed no changes — it only imports `Orchestrator` and `OrchestrationResult`.
- **Single-agent E2E wiring (T016):** `src/orchestrator/orchestrator.ts` now enriches the `AggregatedResponse` with `OrchestrationResult` fields via `Object.assign`. Pipeline: classify → route → dispatchWithTiming → aggregate → enrich. `dispatchWithTiming` wraps each agent call with `performance.now()` to capture per-agent timing in `metadata.agentTimings`. `buildSections` maps agent responses to a `Map<string, string>` keyed by agent name. `extractCitations` pulls citations from three sources: (1) `<cite>` tags in content, (2) `.citations` arrays embedded in JSON content, (3) `metadata.citations` on AgentResponse — all deduplicated via a `Set`. `overallConfidence` comes from classifier confidence; `reasoning` is a human-readable explanation of the routing decision. All T011 single-agent integration tests now pass.
- **Wave 2 Cross-Team Awareness (2026-03-05T20:45):** T013 (River) completed keyword classifier—9/9 tests green. T014 created HttpAgent wrapper in src/agents/http-agent.ts with request/response serialization. T015 implemented AgentRegistry.registerEndpoint()/registerDefaults() factory methods. T016 integrated full pipeline: classify→route→dispatch→aggregate. B5 (Wash) created Python ResponseAggregator in src/orchestrator/aggregator.py with status roll-up and confidence-weighted merging. **US1 complete:** Single-agent routing pipeline end-to-end works. **Track B complete:** All 7 deliverables (B1–B7) done. Zero blockers.
- **Error handling (T025–T028):** ✓ COMPLETE — Orchestrator now enforces per-agent timeouts via `AbortController` + `Promise.race` (default 150ms, configurable via `OrchestratorOptions.perAgentTimeoutMs`). `dispatchWithTiming` returns structured `AgentErrorEntry[]` and `timedOutAgentIds` alongside responses. Post-dispatch validation catches malformed responses (negative confidence → `INVALID_RESPONSE`) and empty results (→ warning). Status determination: all succeed → `"success"`, mixed → `"partial"`, all fail → `"error"` with `mergedContent` set to fallback message. Warnings array surfaces degradation notes for partial results and empty agents. Metadata includes `timedOutAgents: string[]`. Legacy tests fixed by updating stub agents to use `IntentCategory.EXPERTISE_DISCOVERY` (matching classifier fallback). All 55 tests green. Orchestration log: 2026-03-05T2100-kaylee-t025-t028.md
- **Multi-intent dispatch + sectioned aggregation (T020–T022):** ✓ COMPLETE — `Orchestrator.process()` now calls `classifyMulti()` to detect all matching intents, routes each intent through the router, deduplicates agents by id, and dispatches to all matched agents in parallel. The primary (highest-confidence) intent is passed to `dispatchWithTiming` since agents self-select via `canHandle()`. The aggregator's `overallConfidence` (average of successful agent confidences) is now used instead of overriding with classifier confidence. `buildReasoning` updated to list all detected intents when multi-intent. Metadata now includes `classifiedIntents: ClassifiedIntent[]` alongside `classificationResult` (primary). Three test mock classifiers (`TestKeywordClassifier`, two `AlwaysExpertiseClassifier` instances) updated with `classifyMulti` implementations. DefaultAggregator already satisfied T021/T022 from prior work — no aggregator changes needed. All 62 tests green (4 pre-existing T029/T031 red-phase failures unchanged).
- **Citation pipeline completion (T032+T033+T035):** ✓ COMPLETE — `DefaultAggregator` in `src/orchestrator/aggregator/aggregator.ts` now includes: (1) **T032 citationCount** — `metadata.citationCount` set to the length of the deduplicated citations array. The aggregator's existing `extractCitations()` already handled dedup correctly; only the metadata field was missing. (2) **T033 citationCoverage** — new `calculateCitationCoverage()` splits content into sentences (avoiding false splits on abbreviations like "Dr."), classifies sentences as factual if they contain numbers, proper nouns, or `<cite>` tags, counts how many factual sentences have adjacent citations, and produces `citationCoverage = citedFactual / totalFactual`. Added to `metadata.citationCoverage`. When coverage < 1.0, a warning string mentioning "uncited" is pushed to a `warnings` array on the result. (3) **T035 safety filter wiring** — imports `sanitizeRankingLanguage` from `./safety-filter.js`, calls it on each non-error agent's content before section building/citation extraction, collects violations into `metadata.safetyViolations`, and logs via `console.warn` for audit trail. All 71 tests green, 0 failures.

### Wave 3 Completion (2026-03-05T21:00:00Z)

- **T020–T022 DELIVERED:** Multi-intent dispatch pipeline wired into orchestrator. Primary-intent routing with agent deduplication. Aggregator confidence ownership. Metadata enriched with all classified intents. Test mock updates complete. 62/62 target tests green. Zero blockers for citation/safety work.

- **T038 Documentation update:** Rewrote `docs/orchestration-agent-spec.md` from aspirational design spec to implementation-accurate technical documentation. Covers all 8 required sections: Getting Started, Architecture (4-stage pipeline, IntentCategory enum, agent endpoints), Routing Rules (keyword→category mapping), Multi-Intent (classifyMulti, agent dedup), Error Handling (AbortController timeouts, graceful degradation, status determination), Citation Pipeline (extraction, coverage scoring, uncited warnings), Safety Filter (FR-007 prohibited words, sanitizeRankingLanguage), Structured Logging (JSON events per pipeline stage). Updated `docs/INTEGRATION_GUIDE.md` with timeout mechanism details, safety filter notice, and citation format requirements. Updated `docs/DEMO_SCRIPT.md` technical deep-dive section with final feature set. All 71 tests still green.

### Wave 4 Completion (2026-03-05T21:30:00Z)

- **T032–T033+T035 DELIVERED:** Citation pipeline complete. T032 implements `metadata.citationCount` from deduplicated citations. T033 adds `calculateCitationCoverage()` scoring percentage of factual sentences with citations, generates "uncited-claim" warnings when coverage < 1.0. T035 integrates `sanitizeRankingLanguage` safety filter into aggregation pipeline, collecting violations into `metadata.safetyViolations` and logging via `console.warn` for audit trail. All 71 tests green, 0 failures. Citation traceability end-to-end complete.
