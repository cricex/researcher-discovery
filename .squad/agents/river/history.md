# Project Context

- **Owner:** msftsean
- **Project:** orchestration-agent — An orchestration agent that routes user intents to specialized agents, manages an agent registry, and aggregates responses
- **Stack:** TypeScript, Node.js
- **Work Areas:** Orchestrator Core, Intent Classifier, Agent Registry, Response Aggregator, UI, Integration Testing
- **Created:** 2026-03-05

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- **T017 — Multi-intent classification tests (TDD red phase):** Created `tests/classifier/multi-intent.test.ts` with 8 tests across three describe blocks: (1) compound queries returning multiple categories — tests for EXPERTISE_DISCOVERY+COLLABORATION_INSIGHT, EXPERTISE_DISCOVERY+POLICY_COMPLIANCE, RESEARCH_OUTPUT+COLLABORATION_INSIGHT, and a 4-domain spanning query expecting 3+ categories; (2) confidence scores — verifies each returned intent has confidence in [0.3, 1.0] range and preserves rawInput; (3) single-intent regression — ensures single-keyword queries still return exactly one category and fallback behavior is preserved. All 8 tests fail with `classifier.classifyMulti is not a function` — the method doesn't exist yet. T019 will add `classifyMulti()` to `DefaultClassifier` to turn these green. All 9 existing classifier tests remain passing (no regression).

- **T001 — Intent taxonomy pivot to research domain (2026-03-05):** Replaced code-centric `IntentCategory` values (CODE_GENERATION, CODE_REVIEW, EXPLANATION, REFACTOR, TEST_GENERATION, DOCUMENTATION, DEBUGGING) with research domain categories (EXPERTISE_DISCOVERY, RESEARCH_OUTPUT, COLLABORATION_INSIGHT, POLICY_COMPLIANCE). GENERAL was kept. Added optional `context` field to `ClassifiedIntent` with `goldenRecordIds`, `keywords`, and `sessionId`. This is a breaking change — downstream tests referencing removed enum members need updating (specifically `tests/agents/registry.test.ts`). The classifier itself (`src/orchestrator/classifier/classifier.ts`) only used `GENERAL` so it compiled clean.
- **Key file:** `src/specs/intent.ts` is the single source of truth for intent taxonomy (per Decision 1.1 in `.squad/decisions.md`).
- **tsconfig excludes tests:** `tsconfig.json` excludes `tests/` — so `tsc --noEmit` won't catch test-file breakage. Vitest catches it at runtime instead.
- **Team Updates (Phase 1 Setup):** Kaylee created `src/specs/agent-contract.ts` with AgentErrorCode, AgentRequest, AgentContractResponse, AgentErrorResponse, AgentHealthResponse types (camelCase properties with HTTP client handling snake_case mapping). Wash extended `src/specs/response.ts` with Citation, OrchestrationResult, AgentErrorEntry interfaces. All 15 response tests passing. New IntentCategory values are EXPERTISE_DISCOVERY, RESEARCH_OUTPUT, COLLABORATION_INSIGHT, POLICY_COMPLIANCE, GENERAL.
- **T006 — Routing rules configuration (2026-03-05):** Created `src/orchestrator/classifier/routing-rules.ts` exporting `ROUTING_RULES` as `Map<IntentCategory, string[]>`. Maps four intent categories to lowercase keyword patterns for Phase 1 substring-based classification. GENERAL is deliberately excluded — it's the fallback. Added re-export to `src/orchestrator/classifier/index.ts` barrel. JSDoc notes these are replaceable by NLP/ML later. Clean compile; no new test failures (pre-existing `registry.test.ts` failure is the known T001 breakage).
- **Team Updates (Phase 2 Foundation):** Kaylee created `src/agents/endpoints.ts` with AgentEndpointConfig interface and DEFAULT_AGENT_ENDPOINTS Map for three research domain agents (ports 5001-5003). Kaylee created `src/agents/client.ts` with HttpAgentClient using native fetch, AbortController timeouts, and camelCase↔snake_case mapping. Zoe expanded test helpers with three new factories and updated registry.test.ts — all 15 tests passing. Phase 2 foundation infrastructure complete.
- **T010 — Classifier TDD tests (red phase):** Added 6 keyword-based classification tests in a `describe('Keyword-based classification')` block in `tests/classifier/classifier.test.ts`. Tests cover: EXPERTISE_DISCOVERY ("who works on"), RESEARCH_OUTPUT ("publications about"), POLICY_COMPLIANCE ("compliance"/"IRB"), unknown-query fallback to EXPERTISE_DISCOVERY, confidence ≥0.8 for keyword match, confidence ≤0.2 for fallback. 5 of 6 new tests fail against the stub (expected TDD red). The fallback confidence test passes coincidentally since stub returns 0.1. All 3 original DefaultClassifier tests still pass. T013 (classifier implementation) will turn these green.
- **Wave 1 Cross-Team (2026-03-05T20:30):** T010 classifier tests (red phase) depend on T006 routing-rules from Kaylee's Phase 2 work. Zoe's T011/T012 integration + contract tests create the testing framework. Kaylee's B1 FastAPI backend wraps the orchestrator core. Wash's B2/B3 React frontend connects to B1. Mal's B6/B7 documentation guides the integration. All agents executed in parallel across two tracks (A: TDD, B: Demo Layer).
- **T013 — Keyword-based intent classifier (2026-03-05):** Replaced the `DefaultClassifier` stub with real keyword-matching logic. Imports `ROUTING_RULES` from `routing-rules.ts`, scans input case-insensitively against each category's keyword patterns, and returns the highest-confidence match (0.9 for keyword hit). Falls back to `EXPERTISE_DISCOVERY` at 0.1 confidence when no keywords match (per spec edge case). Matched keywords are extracted into `parameters.keywords`. Updated the old "stub behavior" test to expect `EXPERTISE_DISCOVERY` instead of `GENERAL` since the stub is replaced. All 9 classifier tests pass (TDD green phase). 4 pre-existing integration/orchestrator test failures remain — these are downstream pipeline wiring issues (T011), not classifier-related.
- **B4 — Advanced Python routing with confidence scores + multi-turn detection (2026-03-05):** Created `src/orchestrator/router.py` with `IntentRouter` class, `IntentResult` and `RoutingDecision` dataclasses. Provides `classify_intent()` with per-category confidence scoring (fraction of keyword hits scaled to 0.5–0.95 range), `detect_multi_turn()` for follow-up phrase/pronoun detection, `select_agents()` for intent→agent mapping with deduplication, and `route()` as the full pipeline entry point. Keyword patterns mirror the TypeScript `ROUTING_RULES` from `src/orchestrator/classifier/routing-rules.ts` for cross-codebase consistency. Fallback is `expertise_discovery` at 0.3 confidence when no keywords match. Updated `orchestrator.py` to import and delegate to `IntentRouter`, replacing inline `_classify_intent` and `_determine_agent_calls` methods. `route_query()` now accepts optional `conversation_history` for multi-turn support. All 6 router smoke tests pass; 27/29 TypeScript tests pass (2 pre-existing failures unchanged).
- **Wave 2 Cross-Team Awareness (2026-03-05T20:45):** T013 (River) replaced ClassifierStub with KeywordClassifier—all 9 tests pass. T014 (Kaylee) created HttpAgent adapter for REST endpoints. T015 (Kaylee) implemented AgentRegistry with registerEndpoint/registerDefaults factory methods. T016 (Kaylee) wired full classify→route→dispatch→aggregate pipeline—both T011 integration tests pass. B5 (Wash) created Python ResponseAggregator with status roll-up and confidence-weighted merging. **Result:** US1 single-agent query routing complete; Track B demo layer (B1–B7) 100% done. Zero blockers for Wave 3.
- **T019 — Multi-label intent classifier (TDD green phase):** Added `classifyMulti(input: string): Promise<ClassifiedIntent[]>` to `DefaultClassifier` in `src/orchestrator/classifier/classifier.ts` and to the `IntentClassifier` interface in `src/specs/pipeline.ts`. Scans ALL categories in ROUTING_RULES (not just best match), creates a `ClassifiedIntent` per matching category with confidence 0.9, sorts results by confidence descending, and falls back to EXPERTISE_DISCOVERY at 0.1 when nothing matches. Added `'potential'` to the EXPERTISE_DISCOVERY keyword list in `src/orchestrator/classifier/routing-rules.ts` — the T017 test for "Summarize potential collaborators and funding opportunities" required EXPERTISE_DISCOVERY coverage, and "potential" is a legitimate expertise discovery signal (finding potential collaborators/researchers). All 8 T017 multi-intent tests pass, all 9 T010 classifier tests still pass (zero regression). 11 pre-existing failures in error-handling (T023/T024) and integration/orchestrator wiring remain unchanged — those are other teams' TDD red-phase tests. Python `router.py` keyword list may need "potential" added for cross-codebase parity (not done here — out of scope).
- **Multi-intent verification (post-T019):** Re-ran all 8 multi-intent tests (`tests/classifier/multi-intent.test.ts`) — all green. Full suite: 44/55 pass. The 11 failures are unchanged pre-existing TDD red-phase tests in error-handling (T023: 4 timeout tests, T024: 5 degradation tests) and pipeline wiring (1 orchestrator, 1 integration). Zero classifier regressions. The `classifyMulti` method, `IntentClassifier` interface update, and `'potential'` keyword addition from T019 are all correctly wired.
- **Wave 2 Phase 4 (2026-03-05T20:58):** T019 multi-label classifier extended `classifyMulti()` with full keyword scanning across all EXPERTISE_DISCOVERY, RESEARCH_OUTPUT, COLLABORATION_INSIGHT, and POLICY_COMPLIANCE categories. Enhanced keyword detection with "potential" signal for early-stage research discovery. All 17 classifier tests green (9 original + 8 new multi-label). Zoe completed T023+T024 red-phase TDD suite for error handling (9 tests, all failing as expected). Both agents executed in parallel; no blockers. T019 foundation ready for Wave 3 multi-agent task delegation logic.
- **T034 — Ranking language safety filter (2026-03-05):** Created `src/orchestrator/aggregator/safety-filter.ts` implementing FR-007 prohibited ranking word detection. Exports `PROHIBITED_RANKING_WORDS` (8 words: best, top, leading, foremost, premier, preeminent, renowned, distinguished) and `sanitizeRankingLanguage(content)` which returns `{ sanitized, violations }`. Uses context-sensitive regex matching — only flags prohibited words when used as adjectives before nouns, not in prepositional phrases ("on top of") or verb contexts ("is leading researchers"). Neutralization replaces the superlative and inserts "the" if no article precedes. Added export to `src/orchestrator/aggregator/index.ts` barrel. Clean compile, no test regressions (62/66 pass; 4 failures are pre-existing T031 red-phase TDD). Zoe's safety filter tests (`tests/aggregator/safety-filter.test.ts`) don't exist yet — implementation is ready for them.
